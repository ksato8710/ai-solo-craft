# RSSHub self-hosted configuration for AI Solo Craft
#
# IMPORTANT:
# - TWITTER_AUTH_TOKEN is X Web cookie "auth_token" (NOT OAuth token).
# - You can set multiple tokens with comma-separated values.
# - Do NOT commit your actual .env file.

TWITTER_AUTH_TOKEN=replace_with_x_web_auth_token

# Optional: configure the host port for RSSHub container.
RSSHUB_PORT=1200

# Optional cache settings (seconds).
CACHE_EXPIRE=300
CACHE_CONTENT_EXPIRE=3600

# Optional fallback: X official developer API (pay-per-use).
# Keep these empty if you want low-cost auth_token-only mode.
# TWITTER_CONSUMER_KEY=
# TWITTER_CONSUMER_SECRET=
# TWITTER_ACCESS_TOKEN=
# TWITTER_ACCESS_SECRET=

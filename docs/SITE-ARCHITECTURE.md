# AI Solo Builder — サイト構成

*作成日: 2026-02-03*

---

## 技術スタック

| 要素 | 技術 |
|------|------|
| フレームワーク | Next.js (App Router) |
| 言語 | TypeScript |
| スタイリング | Tailwind CSS |
| ビルド | SSG（Static Site Generation） |
| ホスティング | Vercel（無料枠） |
| コンテンツ | Markdown（frontmatter + body） |
| デプロイ | git push → Vercel自動デプロイ |
| リポジトリ | ksato8710/ai-solo-builder |
| ドメイン | ai.essential-navigator.com |

---

## ディレクトリ構成

```
ai-solo-builder/
├── src/
│   ├── app/
│   │   ├── page.tsx          # トップページ
│   │   ├── layout.tsx        # ルートレイアウト
│   │   ├── news/
│   │   │   └── [slug]/
│   │   │       └── page.tsx  # 記事詳細ページ
│   │   ├── category/
│   │   │   └── [category]/
│   │   │       └── page.tsx  # カテゴリ一覧ページ
│   │   └── news-value/
│   │       └── page.tsx      # NVA一覧ページ
│   ├── lib/
│   │   ├── posts.ts          # Markdown読み取り・パース
│   │   └── research.ts       # NVA評価データ読み取り
│   ├── data/
│   │   └── tools.ts          # ツールディレクトリデータ（67件）
│   └── content/
│       └── posts/            # ※ content/news/ からの移行検討
├── content/
│   └── news/                 # 記事Markdownファイル
├── research/                 # NVA中間資料
│   └── YYYY-MM-DD-slug/
│       ├── assessment.md     # NVA評価結果
│       └── sources.md        # ソースデータ
├── docs/                     # 事業設計文書
├── public/
│   └── images/               # 静的画像
├── CLAUDE.md                 # プロジェクト設定
├── EDITORIAL.md              # 編集ガイドライン
└── README.md
```

---

## ページ構成

| パス | ページ | 内容 |
|------|--------|------|
| `/` | トップページ | ヒーロー + カテゴリ別グリッド |
| `/news/[slug]` | 記事詳細 | 記事本文 + NVA評価 |
| `/category/[category]` | カテゴリ一覧 | カテゴリ別記事リスト |
| `/news-value` | NVA一覧 | 全記事のNVAスコア一覧 |

---

## コンテンツ（Markdown）仕様

### Frontmatter

```yaml
---
title: "記事タイトル"
slug: "url-friendly-slug"
date: "YYYY-MM-DD"
category: "morning-news"  # morning-news | featured-tools | deep-dive | case-study
description: "記事の要約（120文字以内）"
readTime: 5               # 読了時間（分）
featured: false            # トップページのヒーロー表示
image: "/images/xxx.jpg"   # OGP画像（オプション）
---
```

### カテゴリ

| slug | 名称 | カラー |
|------|------|--------|
| morning-news | 🌅 朝のAIニュース | #3B82F6 |
| featured-tools | 🛠️ 注目ツール | #8B5CF6 |
| deep-dive | 🔬 深掘り・ハウツー | #10b981 |
| case-study | 📊 事例分析 | #f59e0b |

---

## デプロイ手順

```bash
# 記事追加
1. content/news/ に YYYY-MM-DD-slug.md を作成
2. git add -A && git commit -m "記事タイトル" && git push
3. Vercelが自動デプロイ（1〜2分）
4. https://ai.essential-navigator.com/news/[slug] で公開確認
```

### NVA評価データの更新

```bash
# NVA一覧ページへの反映
1. research/YYYY-MM-DD-slug/ にassessment.md + sources.md を保存
2. src/lib/research.ts の PRODUCT_NAMES / ARTICLE_SLUG_MAP を更新
3. git push でデプロイ
```

---

## 外部連携

| サービス | 用途 | 状態 |
|---------|------|------|
| Vercel | ホスティング・デプロイ | ✅ 稼働中 |
| GitHub | ソースコード管理 | ✅ 稼働中 |
| Google Analytics (GA4) | アクセス解析 | ⬜ 未設定 |
| Search Console | 検索パフォーマンス | ⬜ 未設定 |
| Route 53 | DNS（CNAME → Vercel） | ✅ 設定済み |

---

*このサイト構成はCLAUDE.mdの技術仕様と整合。変更時は両方を更新すること。*

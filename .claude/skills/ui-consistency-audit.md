# UI一貫性監査 — 自動品質チェックSkill

## 概要

`ui-design-system.md` の原則に照らして、UIコードの一貫性を自動監査する。

## トリガー

- pre-commit: UIファイル（`*.tsx`, `*.css`）変更時（軽量チェック）
- cron: 毎週金曜 15:00 JST（フルスキャン）
- 手動: 「UI監査」「デザイン一貫性チェック」「UIルール確認」

## チェック対象

### ファイル

```
src/components/**/*.tsx
src/app/**/*.tsx
src/app/globals.css
```

### 除外

```
node_modules/
.next/
*.test.tsx
*.stories.tsx
```

## 監査ルール（ui-design-system.md 準拠）

### 🔴 Critical（コミットブロック）

| ルール | 違反例 | 正解 |
|--------|--------|------|
| border-radius ≥ 16px | `rounded-lg` (8px) | `rounded-2xl` (16px) |
| グラデーション背景使用 | `bg-slate-800` 単色 | `bg-gradient-to-...` |
| insetハイライト必須（カード） | `shadow-lg` のみ | `shadow-lg` + inset |

### 🟡 Warning（警告のみ）

| ルール | 違反例 | 正解 |
|--------|--------|------|
| letter-spacing タイト | 見出しに `tracking-normal` | `tracking-tight` (-0.01em) |
| border-left 非推奨 | `border-l-4` 見出し | グラデーション下線 |
| italic 非推奨 | `italic` blockquote | `not-italic` |

### 🟢 Info（情報提供）

| ルール | 説明 |
|--------|------|
| カラーパレット準拠 | ブランドカラー以外の使用を検出 |
| spacing 一貫性 | 4px単位以外の値を検出 |

## 実行ステップ

### Step 1: 対象ファイル特定

```bash
# pre-commit時: 変更ファイルのみ
git diff --cached --name-only | grep -E '\.(tsx|css)$'

# フルスキャン時: 全対象ファイル
find src -name '*.tsx' -o -name '*.css'
```

### Step 2: パターンマッチング

各ファイルに対して:

```javascript
// Critical: border-radius チェック
const smallRadius = /rounded-(sm|md|lg)\b/g;
// → rounded-xl, rounded-2xl, rounded-3xl に修正提案

// Critical: 単色背景チェック（カード系コンポーネント）
const flatBackground = /bg-(slate|gray|zinc)-[78]00\b/g;
// → グラデーション背景に修正提案

// Warning: border-left チェック
const leftBorder = /border-l-\d/g;
// → border-bottom + gradient に修正提案
```

### Step 3: 結果レポート

```markdown
## UI一貫性監査レポート

### 🔴 Critical (3件)

| ファイル | 行 | 違反 | 修正提案 |
|---------|:--:|------|---------|
| Card.tsx | 15 | `rounded-lg` | `rounded-2xl` |
| Hero.tsx | 42 | `bg-slate-800` | `bg-gradient-to-br from-slate-800/80 to-slate-900/50` |

### 🟡 Warning (2件)

| ファイル | 行 | 違反 | 修正提案 |
|---------|:--:|------|---------|
| Article.tsx | 23 | `border-l-4` | グラデーション下線 |

### ✅ Pass
- コンポーネント数: 45
- 違反なし: 40 (89%)
```

### Step 4: 自動修正（オプション）

`--fix` フラグ付きで実行時、Criticalルールを自動修正:

```bash
# 実行例
npm run ui:audit -- --fix
```

## pre-commit 統合

### 軽量チェック（<5秒）

```bash
# .husky/pre-commit に追加
npm run ui:audit:quick
```

チェック内容:
- 変更ファイルのみ
- Criticalルールのみ
- 違反があれば exit 1

### 設定例

```javascript
// scripts/ui-audit.mjs
const CRITICAL_RULES = [
  { pattern: /rounded-(sm|md|lg)\b/g, fix: 'rounded-2xl' },
  { pattern: /bg-(slate|gray)-[78]00\b/g, message: 'Use gradient background' },
];
```

## フルスキャン出力

週次cronで実行し、#tifa に報告:

**親メッセージ:**
```
🎨 週次UI監査完了（2026-XX-XX）
- スキャンファイル: 45件
- 一貫性スコア: 89%
- 要修正: 5件
```

**スレッド詳細:**
- 違反一覧
- 修正提案
- 前週比較

## ブランドカラー定義

監査時に参照するカラーパレット:

```css
/* 許可されたカラー */
--brand-blue: #3B82F6;
--brand-violet: #8B5CF6;
--brand-emerald: #10b981;
--brand-orange: #F97316;
--brand-amber: #f59e0b;

/* 背景系 */
--bg-dark: #0f172a;
--bg-card: #1e293b;
--bg-card-hover: #334155;

/* テキスト系 */
--text-primary: #e2e8f0;
--text-secondary: #94a3b8;
```

## 関連ドキュメント

- `ui-design-system.md` — デザイン原則・CSSパターン
- `globals.css` — 実装済みスタイル
- `docs/business/BRAND-IDENTITY.md` — ブランドガイドライン

## フィードバックループ

監査結果は以下に反映:

1. **pre-commit** — Critical違反をブロック
2. **週次レポート** — 傾向分析・改善追跡
3. **ui-design-system.md** — 新ルール追加時に監査ルールも更新
